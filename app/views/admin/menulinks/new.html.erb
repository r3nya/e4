<% provide :title, (t 'objects.menulink.creation') %>

<header>
  <h4><%= t 'objects.menulink.creation' %></h4>
</header>

<article>
  <%= render 'error_messages' %>
  <%= form_for @menulink,
        :url => {
                :controller => :menulinks,
                :action => :create,
        } do |f| %>
    <%= render 'menulink_form', :f => f %>
    <button type="submit"><%= t 'common.actions.add', :obj => (t 'objects.menulink.self') %></button>
  <% end %>
</article>
