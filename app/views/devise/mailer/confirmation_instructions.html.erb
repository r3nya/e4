<p><%= t 'welcome', :user => @resource.nick %></p>

<p><%= t 'confirm_email', :site => Yettings.site_name %></p>

<p><%= link_to t(:confirm_me), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>

<p><%= t(:confirm_decline) %></p>

<p><%= t 'email_signature_html', :team => Yettings.team_name %></p>
